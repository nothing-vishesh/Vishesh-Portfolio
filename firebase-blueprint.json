{
  "entities": {
    "Review": {
      "title": "Review",
      "description": "User feedback and testimonials collected via the live review system.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the person giving the feedback."
        },
        "role": {
          "type": "string",
          "description": "The job title or company of the reviewer."
        },
        "feedback": {
          "type": "string",
          "description": "The detailed testimonial or review text."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the review was submitted."
        },
        "rating": {
          "type": "number",
          "description": "Star rating out of 5."
        },
        "isApproved": {
          "type": "boolean",
          "description": "Admin flag to show/hide the review."
        }
      },
      "required": ["name", "feedback", "createdAt"]
    }
  },
  "firestore": {
    "/reviews/{reviewId}": {
      "schema": "Review",
      "description": "Collection of all client reviews."
    }
  }
}
